version: "3"

services:
    jekyll:
        image: bretfisher/jekyll-serve
        volumes:
            - .:/site
            - cache:/usr/local/bundle
        ports:
            - "8080:4000"

volumes:
    cache:
